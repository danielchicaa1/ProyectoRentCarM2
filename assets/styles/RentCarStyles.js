import { StyleSheet } from 'react-native';

export const rentCarStyles = StyleSheet.create({
  container: {
    margin: 40,
  },
  input: {
    marginBottom: 10,
    padding: 10,
    borderWidth: 1,
    borderColor: 'gray',
    borderRadius: 5,
  },
});
